apiVersion: apps/v1
kind: Deployment
metadata:
  name: fun-taxi-driver-service
  labels:
    app: fun-taxi-driver-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fun-taxi-driver-service
  template:
    metadata:
      labels:
        app: fun-taxi-driver-service
    spec:
      containers:
        - name: fun-taxi-driver-service
          image: whattablackhole/fun-taxi-driver_service:latest
          ports:
          - containerPort: 80
          envFrom:
          - configMapRef:
              name: fun-taxi-driver-service-config
          - secretRef:
              name: fun-taxi-driver-service-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"