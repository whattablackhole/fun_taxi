apiVersion: v1
kind: Service
metadata:
  name: fun-taxi-kafka
  labels:
    app: fun-taxi-kafka
spec:
  clusterIP: None
  selector:
    app: fun-taxi-kafka
  ports:
    - name: fun-taxi-kafka
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093